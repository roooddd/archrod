# ~/.config/swappy/config
[Default]
# O diretório padrão para onde o Swappy irá salvar
save_dir=$HOME/Pictures/screenshots

# O nome padrão do arquivo ao salvar (pode incluir variáveis de data)
# É altamente recomendado usar o formato PNG para screenshots.
save_filename_format=%Y-%m-%d-%H%M%S_swappy.png

# Opcional: Se quiser que o Swappy minimize em vez de fechar
# abort_action=minimize
